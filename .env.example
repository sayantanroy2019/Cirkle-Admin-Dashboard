# Base URL of the Cirkle backend (the one that serves /admin/*).
# In development this is the founder's ngrok tunnel URL, or the backend running
# locally. No trailing slash. Never hardcode this in source.
VITE_API_URL=https://your-ngrok-subdomain.ngrok-free.app
